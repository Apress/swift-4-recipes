var temperature = 30 // Not that cold; it's in Celsius, guys

if temperature < 10 {
	print("Hmm, that's pretty fresh...")
} else if temperature >= 10 && temperature < 30 {
	print("Nice temperature!")
} else {
	print("Hmm, it's getting rather hot...")
}