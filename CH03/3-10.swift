var sunnyWeather = false

// It works like this:
// condition ? do-something : do-something-else

sunnyWeather ? print("Yeah, it's sunny") : print("Too bad...")

// Or we can embed it inside another statement
// Does the exact same thing as the above statement

print(sunnyWeather ? "Yeah, it's sunny" : "Too bad…")