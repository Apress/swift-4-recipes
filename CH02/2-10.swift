// Beginning with 2 forward slashes

/* Or even multi-line, starting with a forward-slash followed by an asterisk
   and ending with an asterisk followed by a forward-slash.

   Yep, just like that. */

/* We can "nest" them, too.
   /* Meaning: too include a comment, within a comment. */
   This sounds rather Inception-ish, I must say.
*/